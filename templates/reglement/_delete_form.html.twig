<form method="post" action="{{ path('Reglement.delete', {'id': reglement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    {#TODO: Mettre modal d'antoine #}
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ reglement.id) }}">
    <button class="btn btn-danger"><i class="fas fa-trash-alt"></i></button>
</form>
