{% extends 'base.html.twig' %}

{% block title %}Modifier un ticket{% endblock %}

{% block body %}
    <h1>Modifier un ticket</h1>

    {{ include('ticket/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('Ticket.show') }}" class="btn btn-info">Retour à la liste</a>

    {{ include('ticket/_delete_form.html.twig') }}
{% endblock %}
