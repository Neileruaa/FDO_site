
{% block title %}{% endblock %}

{% block body %}

    <div >
        <h1> Etes vous certain de vouloir supprimer ce profil ? cette action sera definitive !</h1>

        <form action="{{ path('profile.validDelete') }}">
            <button type="submit" class="btn btn-success">OUI</button>
        </form>
        <form action="{{ path('profile.show') }}">
            <button type="submit" class="btn btn-success">NON</button>
        </form>

    </div>

{% endblock %}
