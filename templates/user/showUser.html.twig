{% extends 'layout.html.twig' %}
{% block body %}

{#   {{ dump(users) }}   #}
    affichage du profil

    <br>



id du club :{{ users.getId() }} <br>
nom du club :{{ users.getUsername() }} <br>
 ville :{{ users.getVilleClub() }} <br>
code postal : {{ users.getCodePostalClub() }} <br>
gerant du club : {{ users.getNameClubOwner() }} <br>
email :{{ users.getEmailClub() }} <br>


<form action="{{ path('User.edit') }}">
    <button type="submit" class="btn btn-success">modifier profil</button>
</form>
    <a  href="{{ path('User.delete', {id: users.id}) }}" class="btn btn-danger text-right float-right">Supprimer ce profil</a>


{% endblock %}


